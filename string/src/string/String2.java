package string;

public class String2 {

	public static void main(String[] args) {
		String str = "Kushagra Verma";
		for(int i = 0; i <= str.length() - 1; i ++) {
		     char start = str.charAt(i);
		     if(start == 'a' || start == 'A') {
		    	 
		     }
		}
		
		for(int i = 0; i <= str.length() - 1; i++) {
			char prnt = str.charAt(i);
				System.out.print(prnt);
		}
	}
}
